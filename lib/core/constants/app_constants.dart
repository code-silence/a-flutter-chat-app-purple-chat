class AppConstants {
  AppConstants._();

  static const String appName = "PurpleChat";

  static const double defaultPadding = 16.0;
  static const double defaultRadius = 14.0;

  static const Duration animationDuration =
      Duration(milliseconds: 250);
}